# Cursor Examples

## Example 1: Agent Mode

```
You: Add JWT auth to the Express app. Update src/middleware/,
     create tests. Run: npm test
```

## Example 2: Inline Edit

Select code → Cmd/Ctrl + K → "Convert to a React hook"

## Example 3: With .cursor/rules

Create `.cursor/rules/general.md` then ask:

```
You: Refactor the auth module following our project rules
```
