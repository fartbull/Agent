# Cline Examples

## Example 1: Code Mode

```
You: Add a CSV export endpoint at /api/export. Use the existing
     patterns in src/routes/. Write tests. Run: npm test
```

## Example 2: Debug Mode

```
You: Find why the goroutine count grows unbounded in src/worker.go
```

## Example 3: With .clinerules

Create `.clinerules` file, then ask:

```
You: Implement the payment service following our rules
```
