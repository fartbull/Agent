# OpenCode Examples

> TODO: These examples are placeholders. OpenCode capabilities need verification.

## Example 1: Basic Task

```bash
opencode "Fix the bug in src/parser.py"
```

## Example 2: Feature

```bash
opencode "Add a health endpoint. Run: npm test"
```
